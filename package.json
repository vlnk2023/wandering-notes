{
  "name": "wandering-notes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python scripts/build-posts.py",
    "deploy": "python scripts/build-posts.py && vercel deploy --prod"
  }
}
